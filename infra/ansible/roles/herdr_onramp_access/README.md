# Herdr onramp access

This role adds only a separate, user-scoped authorized-key file for the
existing onramp Podman stdio relay. It does not rewrite the deploy user's
generic authorized_keys file or change any workload.
