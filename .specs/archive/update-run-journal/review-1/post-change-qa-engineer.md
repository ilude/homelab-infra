# Post-change QA Engineer Review

| Severity | Category | Evidence | Required fix | Confidence |
|---|---|---|---|---|
| medium | validation realism / false pass | Success Criterion 3 and F1 call the four artifacts "sanitized" but their shell check only uses `test -f`. It can pass if `events.jsonl` is malformed, reports are empty, `reflect` wrote a stale or unredacted report, or the artifacts are unrelated to the just-completed `validate` event. The separate fake-fixture tests do not establish these properties for the exact host workflow. | Extend the F1/Success Criterion 3 shell command with deterministic checks that `events.jsonl` parses, contains the generated episode ID and a completed `validate` event with the captured child result, reports reference that event, and each artifact is nonempty. Use a deliberately seeded safe marker in the fake end-to-end fixture to assert retained content and a seeded secret to assert its absence. | high |
| medium | repeated validation / mutable fixture boundary | `just validate` is run inside Success Criterion 3, again through F1, and again through F2. The repository's `just validate` invokes `migrate-values`, which may mutate ignored private values. Thus these are not equivalent read-only repeats, and a later pass can obscure the state that the wrapper actually observed. | Keep one wrapped real `just validate` as the workflow proof. Make F2 run only `git diff --check` and host syntax checks, or explicitly snapshot and compare the allowed private-values migration result before and after each required repeated validation. State why each remaining invocation is necessary. | high |
| medium | archive before/after assertion | F5 postcondition verifies only `plan.md` and `review-1/synthesis.md`. The archive rule requires all sibling review artifacts to move, but no before-move manifest is captured and no post-move check requires the reviewer artifacts, including this required QA artifact. A partial or incorrectly assembled review directory can satisfy F5. | Before moving, generate a sorted relative-file manifest for `.specs/update-run-journal/review-1/` and require the expected reviewer artifacts. After moving, compare that manifest against `.specs/archive/update-run-journal/review-1/`, require the same files, and keep the existing source-absence and archived-plan assertions. | high |
