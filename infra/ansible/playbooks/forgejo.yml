---
- name: Bootstrap Forgejo LXC
  hosts: pve
  gather_facts: false
  roles:
    - role: lxc_ready
      vars:
        lxc_ready_vmid: "{{ forgejo_vmid }}"
        lxc_ready_name: forgejo
    - forgejo
